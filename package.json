{
	"name": "ordo",
	"module": "src/index.ts",
	"type": "module",
	"private": true,
	"engines": {
		"bun": ">=1.3.14"
	},
	"scripts": {
		"dev": "bun run --watch src/index.ts",
		"start": "bun run src/index.ts",
		"sessions": "bun run src/index.ts --sessions",
		"test": "bun test",
		"typecheck": "tsc --noEmit",
		"lint": "biome lint .",
		"format": "biome format --write .",
		"check": "biome check --write .",
		"verify": "biome check --write . && tsc --noEmit && bun test"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.1",
		"@types/bun": "^1.3.14"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@opentui/core": "^0.4.2",
		"@xterm/addon-serialize": "^0.14.0",
		"@xterm/headless": "^6.0.0"
	}
}
